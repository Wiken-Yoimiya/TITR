site_name: TITR 资料库

nav:
  - 主页:
      - 简介: index.md
  - 环境安装:
      - Linux: ubuntu/Ubuntu.md
      - OpenCV: ubuntu/OpenCV.md
      - Azure Kinect DK: ubuntu/Azure Kinect DK 深度相机.md
  - 学习资料:
      - Git: learn/Git.md
theme:
  # 主题
  name: material
  # 语言
  language: zh
  # 切换亮/暗模式。
  palette:
    # 自动模式
    - media: "(prefers-color-scheme)"
      toggle:
        icon: material/brightness-auto
        name: Switch to light mode

    # 亮模式
    - media: "(prefers-color-scheme: light)"
      scheme: default
      toggle:
        icon: material/brightness-7
        name: Switch to dark mode

    # 暗模式
    - media: "(prefers-color-scheme: dark)"
      scheme: slate
      toggle:
        icon: material/brightness-4
        name: Switch to system preference
  # 标签栏设置
  features:
    #顶级部分将呈现在上面视口标题下方的菜单层中，但在移动设备上保持原样
    - navigation.tabs
    - navigation.tabs.sticky
    - navigation.expand # 打开Tab时左侧目录全部展开
    - navigation.top # 返回顶部的按钮 在上滑时出现
    - navigation.path
    - search.highlight # 搜索出的文章关键词加入高亮
    # 代码复制按钮
    - content.code.copy

extra_css:
  - stylesheets/extra.css
  - https://cdn.jsdelivr.net/npm/lxgw-wenkai-webfont@1.1.0/style.css
  # - https://cdn.jsdelivr.net/npm/lxgw-wenkai-lite-webfont@1.1.0/style.css
  # - https://cdn.jsdelivr.net/npm/lxgw-wenkai-tc-webfont@1.0.0/style.css
  # - https://cdn.jsdelivr.net/npm/lxgw-wenkai-screen-webfont@1.1.0/style.cs

extra:
  meta:
    description: "这是 TITR 资料库的官方文档网站"
    keywords: "TITR, 资料库, 文档, MkDocs, Linux, Ubuntu, OpenCV, DK相机, Azure Kinect DK 深度相机"
